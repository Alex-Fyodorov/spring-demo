server:
  port: 8190
  servlet:
    context-path: /book-lib

application:
  reader:
    max-allowed-books: 5

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library
    username: root
    password: EY9fhJLjXok5TQ%
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
  mysql:
    console:
      enabled: true
      settings:
        web-allow-others: false